node_modules/
.env
dist/
*.log
.DS_Store

# RAG databank: raw scraper output + embedding cache are generated, not source.
# Reviewed docs under data/docs/ are intentionally committed.
data/staging/
data/cache/

# LLM config saved from the setup UI (per-deployment, contains an API key).
data/llm-config.json

# Session history + pendingAction persisted at runtime (per-deployment state).
data/sessions.json
